<!-- Space: harukaaibarapublic -->
<!-- Parent: 13_Stateの検査と修正 -->
<!-- Title: terraform state list -->

# terraform state list

State に記録されているすべてのリソースの一覧を表示するコマンド。

---

## 基本的な使い方

```bash
terraform state list
```

出力例：

```
aws_instance.web
aws_security_group.web
aws_s3_bucket.logs
aws_s3_bucket.example["backups"]
aws_s3_bucket.example["logs"]
module.network.aws_vpc.main
module.network.aws_subnet.public[0]
module.network.aws_subnet.public[1]
```

---

## フィルタリング

```bash
# 特定のリソースタイプのみ表示
terraform state list aws_instance.*

# モジュール内のリソースを表示
terraform state list module.network
```

---

## 用途

- 現在 Terraform で管理しているリソースの全体像を把握する
- `state show` や `state rm` の対象リソース名を確認する
- モジュールリファクタリング後に意図したリソース名になっているか確認する
