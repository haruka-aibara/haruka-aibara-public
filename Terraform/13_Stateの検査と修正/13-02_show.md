<!-- Space: harukaaibarapublic -->
<!-- Parent: 13_Stateの検査と修正 -->
<!-- Title: terraform state show -->

# terraform state show

State に記録されている特定リソースの詳細を表示するコマンド。

---

## 基本的な使い方

```bash
terraform state show aws_instance.web
```

出力例：

```hcl
# aws_instance.web:
resource "aws_instance" "web" {
    ami                         = "ami-0c55b159cbfafe1f0"
    arn                         = "arn:aws:ec2:ap-northeast-1:123456789:instance/i-0abc123"
    id                          = "i-0abc123def456789"
    instance_type               = "t3.micro"
    private_ip                  = "10.0.1.100"
    public_ip                   = "54.123.45.67"
    ...
}
```

---

## 用途

- apply 後のリソースの実際の属性を確認する
- ID やリソース ARN を手早く取得する
- `output` に書いていない属性を確認する

---

## for_each / count リソースの場合

```bash
# for_each の場合（キーを指定）
terraform state show 'aws_s3_bucket.example["logs"]'

# count の場合（インデックスを指定）
terraform state show 'aws_instance.web[0]'
```

特殊文字が含まれる場合はシングルクォートで囲む。
