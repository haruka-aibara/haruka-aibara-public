<!-- Space: harukaaibarapublic -->
<!-- Parent: 07_出力 -->
<!-- Title: 出力構文 -->

# 出力値（output）

`output` ブロックで定義した値は `terraform apply` 後に表示され、他のモジュールや外部ツールから参照できる。

---

## 基本的な書き方

```hcl
output "instance_id" {
  value       = aws_instance.web.id
  description = "EC2 インスタンスの ID"
}

output "public_ip" {
  value = aws_instance.web.public_ip
}
```

---

## terraform output コマンド

```bash
# すべての出力を表示
terraform output

# 特定の出力を取得
terraform output instance_id

# JSON 形式で取得（スクリプトや CI/CD との連携に便利）
terraform output -json
```

---

## モジュール間での参照

子モジュールの `output` は、呼び出し元のモジュールから `module.モジュール名.出力名` で参照できる。

```hcl
# モジュール側の outputs.tf
output "vpc_id" {
  value = aws_vpc.main.id
}

# 呼び出し元
module "network" {
  source = "./modules/network"
}

resource "aws_subnet" "app" {
  vpc_id = module.network.vpc_id
}
```

---

## precondition（v1.2+）

出力値に事前条件を設定できる。apply 時に条件を満たさない場合はエラーになる。

```hcl
output "load_balancer_dns" {
  value = aws_lb.main.dns_name
  precondition {
    condition     = aws_lb.main.enable_deletion_protection
    error_message = "本番 LB は削除保護を有効にしてください。"
  }
}
```
