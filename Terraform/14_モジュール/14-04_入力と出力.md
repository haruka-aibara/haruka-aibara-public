<!-- Space: harukaaibarapublic -->
<!-- Parent: 14_モジュール -->
<!-- Title: モジュールの入力と出力 -->

# モジュールの入力と出力

モジュールを呼び出す側は「何を渡せばいいか（入力）」「何が返ってくるか（出力）」だけを知っていればいい。内部実装は見えなくていい——その接点を定義するのが `variable` と `output`。

---

## 入力変数（variable）

モジュールに渡すパラメータ。`variables.tf` に定義する。

```hcl
# modules/ec2/variables.tf
variable "instance_type" {
  type        = string
  description = "EC2 インスタンスタイプ"
  default     = "t3.micro"
}

variable "subnet_id" {
  type        = string
  description = "デプロイ先のサブネット ID"
  # default なし = 呼び出し元から必須で渡す必要がある
}
```

---

## 出力値（output）

モジュールが外部に公開する値。`outputs.tf` に定義する。

```hcl
# modules/ec2/outputs.tf
output "instance_id" {
  value       = aws_instance.main.id
  description = "EC2 インスタンスの ID"
}

output "private_ip" {
  value = aws_instance.main.private_ip
}
```

---

## 呼び出し元での渡し方と参照

```hcl
module "app" {
  source = "./modules/ec2"

  # variable に対応する値を渡す
  instance_type = "t3.small"
  subnet_id     = aws_subnet.app.id
}

# output を参照する
resource "aws_eip" "app" {
  instance = module.app.instance_id
}
```

---

## 設計のポイント

- **必須の変数には default を設定しない** — 呼び出し元が明示的に渡す必要があるため
- **モジュールを使う側が必要な情報だけ output に出す** — 内部の詳細まで全部出力しない
- **変数には description を必ず書く** — モジュールのドキュメント代わりになる
