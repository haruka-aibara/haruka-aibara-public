<!-- Space: harukaaibarapublic -->
<!-- Parent: 07_出力 -->
<!-- Title: 出力構文 -->

# 出力値（output）

`terraform apply` した後、作ったリソースの IP やエンドポイントを確認するためにコンソールを開く必要はない。`output` に定義しておけば apply 完了時に表示されるし、後から `terraform output` で取得できる。モジュール間でデータを受け渡すためにも使う。

---

## 基本的な書き方

```hcl
output "instance_id" {
  value       = aws_instance.web.id
  description = "EC2 インスタンスの ID"
}

output "public_ip" {
  value = aws_instance.web.public_ip
}
```

---

## apply 後に確認する

```bash
# すべての出力を表示
terraform output

# 特定の値だけ取得
terraform output public_ip

# JSON 形式で取得（シェルスクリプトや CI との連携に）
terraform output -json
```

---

## モジュール間でデータを渡す

子モジュールで `output` を定義しておくと、呼び出し元から `module.モジュール名.出力名` で参照できる。

```hcl
# modules/network/outputs.tf
output "vpc_id" {
  value = aws_vpc.main.id
}

# 呼び出し元
module "network" {
  source = "./modules/network"
}

resource "aws_subnet" "app" {
  vpc_id = module.network.vpc_id  # モジュールの出力を参照
}
```

モジュールを分割するときは「何を外に公開するか」を output で明示的に定義する。
