name: Sync to Confluence

on:
  push:
    branches:
      - main
    paths:
      - 'Confluence/**'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create parent pages
        uses: docker://kovetskiy/mark:v15.3.0
        env:
          MARK_USERNAME: ${{ secrets.CONFLUENCE_USER }}
          MARK_PASSWORD: ${{ secrets.CONFLUENCE_API_TOKEN }}
          MARK_BASE_URL: ${{ secrets.CONFLUENCE_BASE_URL }}
        with:
          args: mark -files "Confluence/GitHub-Confluence同期.md"

      - name: Sync all pages
        uses: docker://kovetskiy/mark:v15.3.0
        env:
          MARK_USERNAME: ${{ secrets.CONFLUENCE_USER }}
          MARK_PASSWORD: ${{ secrets.CONFLUENCE_API_TOKEN }}
          MARK_BASE_URL: ${{ secrets.CONFLUENCE_BASE_URL }}
        with:
          args: mark -files "Confluence/**/*.md"
