# R5春 午後Ⅰ

親ページは [演習記録](README.md)。

## 問1 — Java のセキュアプログラミング

解いた日：2026-08-14 ／ 所要時間：33分（14:57〜15:30）／ 結果：11箇所中 正解5・部分正解1（約50%）

Java の Web アプリに対する静的解析とシステムテストで見つかった不具合への対処。初見・未対策の1問目。基準点60%に対して約50%なので、**未対策の1問目としては悪くない立ち位置**。

### 設問別

| 設問 | 解答例 | 自分の解答 | |
|---|---|---|---|
| 1(1) a | 13 | 13 | ✓ |
| 1(2) b | in | fileObj | ✗ |
| 1(3) c | `WHERE head.order_no = ?` | わからん | ✗ |
| 1(3) d | `PreparedStatement stmt = conn.prepareStatement(sql)` | `Statement stmt = conn.createStatement()` | ✗ |
| 2(1) e | orderNo | orderNo | ✓ |
| 2(2) f | static | public | ✗ |
| 2(3) | レースコンディション | レースコンディション | ✓ |
| 2(4) g | `String orderNo` | `private String OrderNo` | △ |
| 2(4) h | new | new | ✓ |
| 2(4) i | `getOrderInfoBean(orderNo)` | `getOrderInfoBean(orderNo)` | ✓ |
| 2(5) j | 得意先コード | 注文番号 | ✗ |

### 落とした原因

**1(3) — JDBC の API が手に入っていない。** プレースホルダの理屈（先に構文を固定するから安全）は説明できるのに、`conn.createStatement()` と書いた。これは脆弱な側そのもの。理屈と API 名が結びついていない。

**2(2)(4) — レースコンディションの用語だけが単独で入っている。** 現象名は即答できたのに、原因（f: static）と対処（g: ローカル変数化）を両方落とした。`private` を付ければ直ると考えており、**可視性の問題と共有の問題を混同**している。

**2(5) — 認可の観点が出てこない。** 「注文番号だけで引ける＝番号を変えれば他人の注文が見える」に気づかず、絞り込みに**足す**項目ではなく**すでに入っている**項目を答えた。

**1(2) — 問題文からの読み取り。** 変数名を本文から拾う問題。知識ではなく精読・時間配分の課題。

### 覚え書き

**JDBC の書き分けは「SQL 文字列をいつ渡すか」で見分ける。**

```java
// 脆弱：SQLは実行時に渡す。文字列連結で組めてしまう
Statement stmt = conn.createStatement();
stmt.executeQuery("SELECT ... WHERE order_no = '" + orderNo + "'");

// 安全：SQLは生成時に渡す。値は後から ? に埋める
PreparedStatement stmt = conn.prepareStatement(sql);  // この時点で構文が確定
stmt.setString(1, orderNo);
stmt.executeQuery();
```

`prepareStatement(sql)` の `(sql)` が「先に構文解析させる」の実体。SQL 側の `?` が「値を入れる場所だけ空けておく」の実体。

**サーブレットのレースコンディションは、f →(3)→ g が一本の話。** サーブレットはインスタンスが1つで、複数リクエストが別スレッドで同時に入る。だから `static` やインスタンスフィールドに値を置くと全リクエストで共有され、他人の値で上書きされる。対処は**メソッド内のローカル変数にすること**（ローカル変数はスレッドごとのスタックに乗るので共有されない）。アクセス修飾子では直らない。生存期間と共有の問題であって可視性の問題ではない。

なお `OrderNo` は Java の命名規約ではクラス名の形。穴埋めは字句一致で採点されるので、変数名は小文字始まりで書く。

**「識別子を直接受け取って、本人のものか確認せずに引く」は午後の頻出パターン。** 名前は認可の欠落／水平権限昇格（IDOR）。設問で「検索条件に何を足すか」を問われたら、**所有者を絞る項目**を探す。今回なら得意先コード。

### 時間

33分。R5春の午後Ⅰは90分で3問中2問選択なので、**持ち時間は1問45分**。12分余った計算になる。現行形式（150分で4問中2問＝1問75分）でも分量増を見込んで余裕がある。

**時間は課題になっていない。伸ばすべきは精度。** ただし逆に、余った時間を見直しに使えていない点は改善余地がある。設問2(5)の「注文番号だけで引ける＝他人の注文が見える」は、書き終えてから読み返せば気づけた可能性がある。本番は途中で切り上げず、持ち時間いっぱい使って見直す。

### 次に活かすこと

- セキュアプログラミング系をもう1問。穴埋めは訓練で伸びる部分
- コードが出る設問では、**修正後のコードを頭の中で動かしてから書く**。`private` を付けても共有が消えないことは、動かして考えれば気づけた
- 検索・参照の処理を見たら「所有者チェックはあるか」を反射的に確認する
- **解き終わっても切り上げない**。持ち時間を使い切って設問を読み直す


## 問2 — セキュリティインシデントのログおよび攻撃の痕跡の調査

解いた日：2026-08-15 ／ 所要時間：26分（11:42〜12:08）／ 結果：10箇所中 正解8（80%）

### 設問別

| 設問 | 解答例 | 自分の解答 | |
|---|---|---|---|
| 1 a | パッシブ | 不明 | ✗ |
| 2(1) b | コミュニティ | 不明 | ✗ |
| 2(2) c | バインド | バインド | ✓ |
| 2(2) 理由 | 2-3によって起動した3-3のポートへの通信が1-3で拒否されているから | 2-3でコマンド実行後、3-3で外部からの接続を待ち、1-3で拒否されたから | ✓ |
| 2(3) d | コネクト | コネクト | ✓ |
| 2(3) 理由 | 2-4によって開始された3-4の通信が1-4で許可されているから | 2-4でコマンド実行後、3-4で外部との接続確立し、1-4で許可されたから | ✓ |
| 2(4) e | 1365 | 1365 | ✓ |
| 3(1) | TXTレコードには任意の文字列を設定できるから | TXTレコードには任意の文字列を記載できるため | ✓ |
| 3(2) | 稼働しているファイルと内容が異なる可能性があるから | C&Cサーバ側のファイルがさし変えられている可能性がある | ✓ |
| 3(3) f | 受付サーバ | 受付サーバ | ✓ |

### 落とした原因

- 設問1と設問2(1)は空欄。本文・図表から用語を特定し切れなかった。

### 覚え書き

### 時間

26分。持ち時間45分に対し19分余った。

### 次に活かすこと

- 空欄のまま終えず、余った時間で本文と図表を再確認する。


## 問3 — クラウドサービス利用時のネットワークセキュリティ

解いた日：2026-08-15 ／ 所要時間：34分（12:18〜12:52）／ 結果：21箇所中 正解13（約62%）

### 設問別

| 設問 | 解答例 | 自分の解答 | |
|---|---|---|---|
| 1(1) a | ア | ア | ✓ |
| 1(1) b | イ | イ | ✓ |
| 1(1) c | ウ | ウ | ✓ |
| 1(2) | 送信元制限機能で、本社のUTMからのアクセスだけを許可しているから | 送信元制限機能が有効であり顧客が設定したIPアドレス以外のアクセスは拒否される | ✓ |
| 2(1) d | ア | ア | ✓ |
| 2(1) e | ウ | ウ | ✓ |
| 2(1) f | イ | イ | ✓ |
| 2(2) g | イ | オ | ✗ |
| 3(1) | プロキシサーバではなく、Pサービスを経由する | 営業所UTMから本社UTMへのVPN通信が不要になる | ✗ |
| 3(2) | 送信元制限機能で、営業所のUTMのグローバルIPアドレスを設定する | 送信元制限機能の顧客設定IPアドレスに営業所IPアドレスを追加する | ✓ |
| 3(3) | イ | ア | ✗ |
| 3(4) h | 6 | 6 | ✓ |
| 3(4) i | 2 | 2 | ✓ |
| 3(5) あ | 4 | 4 | ✓ |
| 3(5) j | `https://△△△-a.jp/` | URL | ✗ |
| 3(5) k | 研究開発部の従業員 | SaaS-a | ✗ |
| 3(5) l | 許可 | 許可 | ✓ |
| 3(5) い | 3 | 3 | ✓ |
| 3(5) m | 外部ストレージサービス | URL カテゴリ | ✗ |
| 3(5) n | 全て | SaaS-a | ✗ |
| 3(5) o | 禁止 | 許可 | ✗ |

### 落とした原因

- 設問3(1)は、アクセス経路の違いではなく、VPN通信が不要になるという結果を答えた。
- 設問2(2)と3(3)は解答群からの選択を誤り、設問3(5)はWebフィルタリング設定表の項目の対応を取り違えた。

### 覚え書き

### 時間

34分。持ち時間45分に対し11分余った。

### 次に活かすこと

- 問われているのが経路・方式・設定値のどれかを確認してから答える。
- 設定表は、各列の見出しと解答欄の対応を一つずつ確認する。
